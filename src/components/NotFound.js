import React from 'react';

export default function NotFound() {
    return (
      <div className="text-center">
        <h2 class="alert alert-danger">No such application option exists!</h2>
        <p>Please select from one of the menu options above</p>
      </div>
    );
  }